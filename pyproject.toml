[project]
name = "dify-status"
version = "0.1.0"
description = "Unofficial status page for Dify Cloud"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "pyyaml>=6.0.3",
]
